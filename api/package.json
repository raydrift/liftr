{
  "name": "rohitfit-api",
  "version": "1.0.0",
  "description": "Workout tracker API — Azure Functions",
  "main": "src/index.js",
  "scripts": {
    "start": "func start",
    "build": "node --check src/shared/tableClient.js && node --check src/sessions/index.js && node --check src/sessions/validation.js && node --check src/session/index.js && node --check src/stats/index.js",
    "test": "node --test test/*.test.js",
    "deploy": "func azure functionapp publish rohitfit-func"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.x"
  },
  "engines": {
    "node": ">=20"
  }
}
